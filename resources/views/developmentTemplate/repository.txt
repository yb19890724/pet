<?php

namespace App\Repositories\{namespace};

use App\Repositories\BaseRepository;
use App\Models\Backend\{name}Model;
use Facades\App\Transformer\{namespace}\{name}Transform as {name};

/* {annotation}仓库层 */
class {name}Repository extends BaseRepository
{
    public $bindModel = {name}Model::class;//绑定模型


    public function {include}s(array $params)
    {
        return $this->model->{include}s($params,'dictionaries');
    }

    public function {include}Create(array $params)
    {
        return $this->model->create($params);
    }


    public function {include}Destroy(int $params)
    {
        return $this->model->destroy($params);
    }


    public function {include}Update(array $params)
    {
        list($where,$attributes)={name}::parameterCondition($params);
        return $this->model->conditionsUpdate($where,$attributes);
    }
}